创建add.js cale.js

npm i -D mocha chai（安装mocha和chai）

创建test文件夹 calc.test.js

修改package.json里的
"scripts": {
    "test": "mocha"
  },

注意Windows有.的要用反斜杠\！！！

执行npm test
(
    其他执行方式：
    (不成功)node_modules/mocha/bin/mocha test/cale.test.js
    node node_modules/mocha/bin/mocha

    node_modules\.bin\mocha
)

查看代码覆盖率
npm i -D istanbul
本来执行 node_modules\.bin\istanbul cover node_modules\.bin\_mocha
不成功 
和istanbul版本有关 
可以配置Gruntfile.js
npm install grunt --save-dev（安装grunt）
执行 grunt cover